# EBMS Frontend

This folder can host a separate SPA if desired. For this exercise, the UI is served by JSPs from the backend. You can later replace JSPs with a modern SPA and consume the backend via REST.

